{
  "name": "@faraday-academy/platform-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Faraday Stage 2 platform ports and use-cases (auth, release, LMS, assessment, tutor, commerce, community).",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@faraday-academy/platform-contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24",
    "tsx": "^4.20.3",
    "typescript": "~5.9.0"
  }
}

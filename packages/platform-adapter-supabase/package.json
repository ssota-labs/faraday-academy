{
  "name": "@faraday-academy/platform-adapter-supabase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Supabase adapter for Faraday platform. P0/P3: memory-backed until Supabase credentials are configured; SQL migrations included.",
  "exports": {
    ".": "./src/index.ts",
    "./migrations": "./migrations/001_init.sql"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@faraday-academy/platform-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24",
    "tsx": "^4.20.3",
    "typescript": "~5.9.0"
  }
}

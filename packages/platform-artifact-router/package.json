{
  "name": "@faraday-academy/platform-artifact-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hostname → course → active release resolution and Course Shell HTML.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@faraday-academy/platform-contracts": "workspace:*",
    "@faraday-academy/platform-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24",
    "tsx": "^4.20.3",
    "typescript": "~5.9.0"
  }
}

{
  "name": "@faraday-academy/platform-studio-sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Isolated studio sandbox worker (secret-free, quota-limited). Vercel Sandbox wiring comes later; local in-process sandbox for P2.",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@faraday-academy/platform-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24",
    "tsx": "^4.20.3",
    "typescript": "~5.9.0"
  }
}
